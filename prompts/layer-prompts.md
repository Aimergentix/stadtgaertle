# Layer Prompts — Stadtgärtle Rheinfelden

> Each section is a self-contained Side-Chat starter for Cursor.
> Workflow per layer: open a clean chat → paste the @mentions → paste the prompt → review.
> Do not begin layer N+1 until layer N is committed to Git.
> Roles: Archon pastes, reads, and commits. Aimergent generates into chat only.

---

## Layer 0 — Documentation review (no Aimergent needed)

**Archon checklist — confirm every item before Layer 1:**

- [ ] `HACE.md` in repository root.
- [ ] `.cursor/rules/hace.mdc` present (new Cursor format, replaces `.cursorrules`).
- [ ] `docs/00_template_guide.md` present.
- [ ] `docs/01_website_spec.md` present (generated by audit-hace.md).
- [ ] `docs/02_design_system.md` present.
- [ ] `docs/03_assembly_line.md` present (generated by audit-hace.md).
- [ ] `docs/04_server_setup.md` present.
- [ ] `project-manifest.md` in root (treat as `docs/00_project_manifest.md` semantically).
- [ ] `prompts/layer-prompts.md` present (this file).
- [ ] All docs read line by line by the Archon. Questions asked in chat before proceeding.

**Gate:** All boxes checked. Commit: `docs: complete layer-0 documentation`.

---

## Layer 1 — Vite foundation (Archon terminal + Aimergent)

**Archon runs these commands first (in order). Each installs cleanly before the next.**

```bash
# In the project root:
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install react-router-dom
npm install -D tailwindcss@3 postcss autoprefixer
npx tailwindcss init -p
npm install -D eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint-plugin-react-hooks
```

Do not run `npx shadcn@latest init` yet — the Aimergent generates `tailwind.config.ts` first,
then the Archon runs shadcn init, then updates `src/index.css` per docs/02_design_system.md §6.

**Side-Chat prompt (open a new, empty Cursor Side-Chat, paste exactly):**

```
@docs/01_website_spec.md @docs/02_design_system.md @HACE.md

Aimergent — the Archon is in Manual Gating mode. Output all files as code blocks in this
chat. Do not write to disk.

Generate six files for the /frontend/ Vite project:

1. tailwind.config.ts — extend colors with the garden palette from docs/02_design_system.md §3.
   Extend fontFamily with the system stack from §1. content: ["./index.html","./src/**/*.{ts,tsx}"].

2. vite.config.ts — React plugin only. Server port 5173. No base path override.

3. src/main.tsx — React 18 createRoot. Wrap in BrowserRouter from react-router-dom.

4. src/App.tsx — React Router v6 Routes. Four routes:
   / → HomePage · /impressum → ImpressumPage · /datenschutz → DatenschutzPage · * → NotFoundPage.
   All four are placeholder imports (empty components). Zero hardcoded text.

5. src/lib/utils.ts — shadcn/ui cn() helper using clsx and tailwind-merge. Under 10 lines.

6. eslint.config.js — ESLint 9 flat config. TypeScript plugin.
   Rules: no-explicit-any error · react-hooks/rules-of-hooks error · no-unused-vars warn.

Each file: zero any, zero TODO, zero prose. Hard cap 200 lines.
Output only the file content. No commentary. No code fences around the whole response.
```

**Gate:** `cd frontend && npm run build` exits zero. Commit: `feat: scaffold layer-1-foundation`.

---

## Layer 2 — Config and types (one Side-Chat per Small World)

> Block: Small Worlds 4–5 (JSON files with German copy) require the Gärtnermeister's data
> (project-manifest §A.6). Types (Small Worlds 1–3) can proceed immediately.

**Small World 1 — src/types/site-content.ts**

```
@docs/01_website_spec.md @project-manifest.md @HACE.md

Aimergent — Manual Gating. Output into chat only.

Generate frontend/src/types/site-content.ts.
Match exactly the schema in project-manifest.md §A.3.
Export SiteContent, SeasonEntry, and RuleEntry as named exports. Under 80 lines.
Output only the file content. No commentary.
```

**Small World 2 — src/types/navigation.ts**

```
@docs/01_website_spec.md @project-manifest.md @HACE.md

Aimergent — Manual Gating. Output into chat only.

Generate frontend/src/types/navigation.ts.
Match exactly project-manifest.md §A.4. Export NavItem and NavConfig. Under 30 lines.
Output only the file content. No commentary.
```

**Small World 3 — src/config/services-router.ts**

```
@docs/01_website_spec.md @project-manifest.md @HACE.md

Aimergent — Manual Gating. Output into chat only.

Generate frontend/src/config/services-router.ts.
Match exactly project-manifest.md §A.5. Use as const. Export services and ServiceKey.
Do not add any URL not listed in §A.5. Under 20 lines.
Output only the file content. No commentary.
```

**Small Worlds 4–5 (JSON): blocked — wait for project-manifest §A.6 data from the Gärtnermeister.**

**Gate:** `npm run build` zero errors after each. Commit each separately.

---

## Layer 3 — Components (one Side-Chat per component, follow manifest order)

**Template prompt — substitute {Name} and {sources} for each row:**

```
@docs/01_website_spec.md @docs/02_design_system.md @HACE.md

Aimergent — Manual Gating. Output into chat only.

Generate frontend/src/components/{Name}.tsx.
Data sources: {sources}.
Zero hardcoded text — all visible copy from imported config or passed props.
Tailwind utility classes only (design system doc §7). Zero CSS-in-JS.
Zero any · zero TODO · under 100 lines.
Output only the file content. No commentary.
```

| {Name}          | {sources}                                   |
|-----------------|---------------------------------------------|
| Layout          | navigation.json, site-content.json          |
| HeroSection     | site-content.json, public/images/hero.webp  |
| AboutSection    | site-content.json                           |
| CalendarSection | site-content.json                           |
| RulesSection    | site-content.json                           |
| PlotMapSection  | site-content.json, public/images/plot-map.png |
| ContactSection  | site-content.json, services-router.ts       |
| Footer          | navigation.json, site-content.json          |
| HomePage        | all section components above                |

**Gate:** `npm run build` zero errors after each. One commit per component.

---

## Layer 4 — Legal pages and 404 (required before any public deploy)

**Archon pre-step:** Add `imprint` and `privacy` keys to `src/types/site-content.ts` and
`site-content.json` in separate Small World Side-Chats before generating the pages.

**Side-Chat prompt:**

```
@docs/01_website_spec.md @docs/02_design_system.md @HACE.md

Aimergent — Manual Gating. Output into chat only.

Generate three files:

1. frontend/src/pages/ImpressumPage.tsx
   Renders §5 TMG Impressum. All text from site-content.json imprint key. Under 60 lines.

2. frontend/src/pages/DatenschutzPage.tsx
   Renders GDPR Datenschutzerklärung. All text from site-content.json privacy key. Under 80 lines.

3. frontend/src/pages/NotFoundPage.tsx
   404 page. Heading + link to /. Text from site-content.json. Under 30 lines.

Zero any · zero TODO · Tailwind only.
Output only the file content. No commentary.
```

**Gate:** Routes /impressum, /datenschutz, and a random path all render after `npm run build`.

---

## Layer 5 — Quality gates (one-time setup after Layer 4)

**Archon terminal:**

```bash
cd frontend
npm install -D prettier eslint-config-prettier
```

**Side-Chat prompt:**

```
@docs/01_website_spec.md @HACE.md

Aimergent — Manual Gating. Output into chat only.

Generate two files:

1. frontend/eslint.config.js — ESLint 9 flat config. TypeScript plugin.
   no-explicit-any: error · react-hooks/rules-of-hooks: error · no-unused-vars: warn.
   Extend eslint-config-prettier last. Under 40 lines.

2. frontend/.prettierrc — JSON. singleQuote true · trailingComma es5 · printWidth 100 · tabWidth 2.

Output only the file content. No commentary.
```

Add `"lint": "eslint ./src"` to package.json scripts.
In `.github/workflows/deploy-static.yml`, add `npx eslint ./src` as a step before `npx tsc --noEmit`.

**Gate:** `npm run lint` zero errors · `npm run build` zero errors.

---

## Layer 6 — Server deploy (Archon terminal only — no Aimergent needed)

Follow `docs/04_server_setup.md` exactly, in phase order (4.1 → 4.2 → 4.3 → 4.4 → 4.5).

**Acceptance checklist:**

- [ ] Phase 4.1: SSH key pair generated. Public key on server. Private key + 4 secrets in GitHub.
- [ ] Phase 4.2: deploy user exists. `/var/www/stadtgaertle` owned by deploy.
- [ ] Phase 4.3: nginx config applied. `nginx -t` passes.
- [ ] Phase 4.4: Certbot cert issued. `certbot renew --dry-run` passes.
- [ ] Phase 4.5: Push to main. GitHub Actions job green.
  `curl https://stadtgaertle.rheinfelden.de/impressum` → HTTP 200 ✓
